#include <stdio.h>
int main()
 {
    double result = 1.0;
    int i;

    if (exponent > 0) {
        for (i = 1; i <= exponent; i++) {
            result *= base;
        }
    } else if (exponent < 0) {
        for (i = 1; i <= -exponent; i++) {
            result /= base;
        }
    }

    return result;

